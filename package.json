{
  "name": "maetse-site",
  "version": "1.0.0",
  "private": true,
  "description": "Personal site for Christine Mae Tse, generated from a single source of truth (data/site.json).",
  "scripts": {
    "build": "node build.js",
    "check": "node build.js --check"
  }
}
